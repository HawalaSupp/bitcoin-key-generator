import SwiftUI

// MARK: - Scheduled Transactions View (Legacy Stub)
// Replaced by ScheduledTransactionsOverlay.swift in UI/
// Original backed up as ScheduledTransactionsView.swift.bak

struct ScheduledTransactionsView: View {
    var body: some View {
        Text("Migrated to ScheduledTransactionsOverlay")
            .foregroundColor(.secondary)
    }
}
